## 1. Test 斷言先行（RED）

- [x] 1.1 [P] 更新 tests/config.sh 內 4 處以 `gpt-5.5` 為預期 default 的斷言為 `gpt-5.6-sol`（含 no-profile scenario 的 `| model | gpt-5.6-sol | (default) |` row 斷言與 DEFAULTS 迴圈清單）。行為：config 測試自此以 `gpt-5.6-sol` 為 canonical default。驗證：`bash tests/config.sh` 在 SKILL.md 尚未更新時 FAIL（RED 確認斷言真的生效），SKILL.md 更新後 PASS。
- [x] 1.2 [P] 更新 tests/adversarial-review.sh 內 resolver 斷言字串 `gpt-5.5|xhigh|600|security|project` 與 `gpt-5.5|xhigh|600||default` 為 `gpt-5.6-sol|…` 對應值，並同步 tests/status.sh 內 3 處 fixture 的 `model: gpt-5.5` 為 `model: gpt-5.6-sol`。行為：adversarial-review resolver 測試與 status fixture 反映新 default。驗證：`bash tests/adversarial-review.sh` 於 SKILL.md 更新前 FAIL、更新後 PASS；`bash tests/status.sh` 全程 PASS（fixture 值對 status 是 opaque 顯示值）。

## 2. Producer / consumer SKILL.md 值替換（GREEN）

- [x] 2.1 [P] 將 plugins/codex-pro/skills/codex-review/SKILL.md 內 6 處 `gpt-5.5`（frontmatter 版本說明、Step 4.1 resolver `DEFAULTS` dict、`--model` flag 說明、result-file 範例、fallback 說明）替換為 `gpt-5.6-sol`。行為：未設 profile 的 `/codex-pro:codex-review` 對 codex-call 送出 `--model gpt-5.6-sol`。驗證：`bash tests/review.sh` 與 `bash tests/config.sh` PASS；`grep -c 'gpt-5.5' plugins/codex-pro/skills/codex-review/SKILL.md` 輸出 0。（對應 spec requirement：Review invocation uses codex-call HTTPS direct without subprocess for Codex）
- [x] 2.2 [P] 將 plugins/codex-pro/skills/codex-rescue/SKILL.md 內 6 處 `gpt-5.5` 替換為 `gpt-5.6-sol`（同 2.1 的位置類型）。行為：未設 profile 的 `/codex-pro:codex-rescue` 對 codex-call 送出 `--model gpt-5.6-sol`。驗證：`bash tests/rescue.sh` PASS；`grep -c 'gpt-5.5' plugins/codex-pro/skills/codex-rescue/SKILL.md` 輸出 0。（對應 spec requirements：Rescue invocation uses codex-call HTTPS direct without subprocess for Codex；Rescue output is a structured Markdown result file — result-file 範例 frontmatter 的 model 值同步）
- [x] 2.3 [P] 將 plugins/codex-pro/skills/codex-adversarial-review/SKILL.md 內 6 處 `gpt-5.5` 替換為 `gpt-5.6-sol`（同 2.1 的位置類型，含 focus_default resolver 說明）。行為：未設 profile 的 `/codex-pro:codex-adversarial-review` 對 codex-call 送出 `--model gpt-5.6-sol`。驗證：`bash tests/adversarial-review.sh` PASS；`grep -c 'gpt-5.5' plugins/codex-pro/skills/codex-adversarial-review/SKILL.md` 輸出 0。（對應 spec requirements：Adversarial-review invocation uses codex-call HTTPS direct without subprocess for Codex；Adversarial-review output is a structured Markdown result file with four mandatory non-empty sections — result-file 範例 frontmatter 的 model 值同步）
- [x] 2.4 [P] 將 plugins/codex-pro/skills/codex-config/SKILL.md 內 4 處 `gpt-5.5`（schema default 表、resolved-profile 範例輸出、profile.yaml 範例、hardcoded default 說明）替換為 `gpt-5.6-sol`。行為：`/codex-pro:codex-config` 於無 profile 環境顯示 `| model | gpt-5.6-sol | (default) |`。驗證：`bash tests/config.sh` PASS；`grep -c 'gpt-5.5' plugins/codex-pro/skills/codex-config/SKILL.md` 輸出 0。（對應 spec requirements：Config profile resolution algorithm — two-layer with project priority；Config v0.1 schema is exactly 4 fields）
- [x] 2.5 將 plugins/codex-pro/skills/codex-batch/SKILL.md 內 2 處 `gpt-5.5`（default model 記載、troubleshooting 的 `-m gpt-5.5` 範例）與 plugins/codex-pro/skills/codex-batch/references/script-template.sh 內 1 處 `gpt-5.5` 範例註解替換為 `gpt-5.6-sol`，並將 tests/static.sh 與 tests/batch.sh 內**各自獨立**的 hardcoded template sha256（雙重驗證設計，兩份 copy）更新為新 template 內容的實際 hash（`shasum -a 256` 重算）。行為：batch 互動蒐集的 model default 為 `gpt-5.6-sol`，template invariant 檢查對新內容通過。驗證：`bash tests/static.sh` PASS（sha256 比對新 hash）；`bash tests/batch.sh` PASS。（對應 spec requirement：Batch skill registration and parameter collection）

## 3. Docs 記載同步

- [x] 3.1 [P] 將 README.md 內 1 處與 CLAUDE.md 內 1 處 `gpt-5.5` default 記載替換為 `gpt-5.6-sol`（CLAUDE.md 的 Design constraint #5 段落「gpt-5.5 / xhigh / 600」→「gpt-5.6-sol / xhigh / 600」）。行為：對外文件與實際 default 一致。驗證：`grep -rn 'gpt-5.5' README.md CLAUDE.md` 輸出空。

## 4. 全量驗證與 smoke gate

- [x] 4.1 跑 `bash tests/run.sh`，Layer 1+2 aggregate 全綠（0 fail）。行為：全部既有 assertions 在新 default 下通過。驗證：終端輸出 aggregate 行顯示 `0 fail` 且 exit 0。
- [x] 4.2 Per-ship smoke：以無 profile 環境各跑一次真 codex-call 的 3 個 producer skill（review 對小 diff、rescue 對 trivial task、adversarial-review 對小 target、各加 `--max-time 300` 內的合理上限），確認 (a) result file frontmatter `model: gpt-5.6-sol`、(b) review 輸出含 `## Summary` 與 `## Findings` 兩個 H2、(c) adversarial-review 輸出含 4 個 mandatory H2 sections 各非空、(d) rescue 輸出含 `## Task Brief` / `## Outcome` / `## Suggested Next Steps`。行為：heading contract 在 `gpt-5.6-sol` 下不漂移（issue #1 的 drift 風險點）。驗證：人工檢視 3 個 `.codex-pro/*.md` result files 的 frontmatter 與 H2 headings；任一漂移即回頭調整該 SKILL.md 的 Step 3 literal-token instructions 並重跑。
- [x] 4.3 殘留掃描：`grep -rn 'gpt-5.5' --include='*.md' --include='*.sh' plugins/ tests/ README.md CLAUDE.md` 輸出空（openspec/changes/archive/ 與 openspec/changes/harden-producer-heading-reliability/ 的歷史/在途記載刻意不動，不在掃描範圍）。行為：live surface 無舊 default 殘留。驗證：上述 grep 命令輸出空且 exit 1（no match）。
